using UnityEngine;
using System.Collections;
using UnityEngine.EventSystems;

// Since it's not actually a ship tile, don't inherit from ShipTile.
public class PotentialShipTile : MonoBehaviour, IPointerEnterHandler, IPointerClickHandler {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
